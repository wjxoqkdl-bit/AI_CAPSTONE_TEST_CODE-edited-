#..streamlit/config.toml

[theme]
# 기본 상호작용 요소(버튼, 슬라이더 등)의 기본 색상
primaryColor="#1E88E5"

# 메인 콘텐츠 영역의 배경색
backgroundColor="#FFFFFF"

# 사이드바 등 부차적인 영역의 배경색
secondaryBackgroundColor="#F5F5F5"

# 기본 텍스트 색상
textColor="#31333F"

# 사용할 폰트 (sans serif, serif, or monospace 중 선택)
font="sans serif"